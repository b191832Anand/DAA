#include<bits/stdc++.h>
#include<time.h>
using namespace std;
int main()
{
	int n;
	cin>>n;
	srand(time(nullptr));
	cout<<setprecision(8)<<fixed<<endl;
	clock_t time;
	time=clock();
	vector<int>v;
	for(auto i=0;i<1000#include <.h>;i++)
{
	  int x=(rand()%(10-1+1))+1;
	  v.push_back(x);
}
    time=clock()-time;
    double timetaken=((double)time/(CLOCKS_PER_SEC));
    cout<<timetaken<<endl;
    for (auto &i:v)
    {
    	cout<<i<<"  ";
    }
#include <#include <#include <.h>>>