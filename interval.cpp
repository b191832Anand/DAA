#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n;
	vector<vector<int>>ans(n,vector<int>(2));
	for(int i=0;i<n;i++){
		cin>>ans[i][0]>>ans[i][1];
	}
	sort(ans.begin(),ans.end());
	vector<vector<int>>temp;
	for(int i=0;i<n;i++){
		int start=ans[i][0];
		int end=ans[i][1];
		if(!temp.empty()&&end<=ans.back()(1));
		continue;
		for(int j=i+1;j<n;j++)
		{
			
		}
	}
}